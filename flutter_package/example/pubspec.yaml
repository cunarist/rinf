name: example_app
description: Demonstrates how to use the Rinf plugin
publish_to: "none"

version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  rinf:
    path: ../
  cupertino_icons: ^1.0.8
  meta: ^1.15.0
  tuple: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
