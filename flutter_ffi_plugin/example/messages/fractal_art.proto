syntax = "proto3";
package fractal_art;

import "counter_number.proto";

// [RINF:RUST-SIGNAL]
// You can add your custom comments like this.
// Protobuf's import statement also works well.
message SampleFractal {
  double current_scale = 1;
  counter_number.SampleSchema dummy = 2;
}
